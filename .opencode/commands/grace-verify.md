---
name: "grace: Verify"
description: "Run 3-level GRACE verification"
---
Call the MCP tool `verify_project` at level="module-local". If it passes, continue. If it fails, call `grace_fix` to resolve issues before proceeding.
