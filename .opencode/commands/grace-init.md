---
name: "grace: Init"
description: "Initialize GRACE architecture for a new project"
---
Call the MCP tool `grace_init`. Create the sharded GRACE layout: graph-index.xml, plan-index.xml, verification-index.xml, module/phase/verification shard directories. Set up .opencode integration. Do this BEFORE writing any code.
