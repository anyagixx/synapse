---
name: "grace: Fix"
description: "Diagnose and fix a bug with GRACE evidence"
---
Call the MCP tool `grace_fix` with the issue description and module hint. Read failure evidence from logs. Find the root cause via semantic_search + graphrag_query. Apply the fix, verify, review, and refresh artifacts.
