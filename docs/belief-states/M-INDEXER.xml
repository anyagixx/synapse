<?xml version="1.0" encoding="UTF-8"?>
<BELIEF_STATE module="M-INDEXER" version="1.0">
  UNDERSTANDING:
    Module responsibility: Code indexer — walks, delegates pipeline block construction, attaches optional embeddings, stores full or delta snapshots, and searches code blocks with guarded storage health checks
    Key data flows:
      - INPUT: Use MODULE_CONTRACT, sharded docs, and execution context: Phase-14 baseline: read MODULE_CONTRACT, module shard, verification shard, and product traceability defaults before public release.
      - PROCESSING: Implement only the declared scope while preserving dependencies: M-INDEXER-EMBEDDING, M-INDEXER-WALKER, M-INDEXER-PARSER, M-INDEXER-STORAGE, M-INDEXER-STORAGE-SEARCH, M-INDEXER-STORAGE-TYPES, M-CONFIG
      - OUTPUT: Indexed search results, including filtered lexical/semantic search entrypoints, that verify against MyGRACE gates
    Critical invariants:
      - Scope remains bounded to: Indexer struct, SearchResult, guarded storage locks, index_directory, index_delta, gitignore-aware indexing, best-effort embedding attachment, stale-entry pruning, filtered search, hybrid_search, storage health propagation, view_signatures
      - Embedding failures must never prevent deterministic lexical block persistence.
      - MODULE_CONTRACT, MODULE_MAP, CHANGE_SUMMARY, and semantic anchors stay synchronized
  IMPLEMENTATION_STRATEGY:
    Approach: Read sharded artifacts first, preserve public contracts, make scoped edits, then run verification gates
    Complexity_areas: artifact drift, backward compatibility, verification coverage
    Patterns_applied: explicit data flow, typed semantic links, bounded semantic blocks
  VERIFICATION_INTENT:
    I expect the following to be true after my code runs:
    - belief-state-exists reports no malformed BELIEF_STATE blocks
    - verify_project, review_code, and relevant unit tests pass after implementation
  RISKS_ACKNOWLEDGED:
    - This scaffold is generated from artifacts and should be refined by the implementing agent before substantial code generation
    - Incomplete module artifacts can make the first belief state too generic
</BELIEF_STATE>
