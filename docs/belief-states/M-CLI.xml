<?xml version="1.0" encoding="UTF-8"?>
<BELIEF_STATE module="M-CLI" version="1.0">
  UNDERSTANDING:
    Module responsibility: CLI schema facade — clap-powered top-level parser, command enum, command dispatch, and command argument structs
    Key data flows:
      - INPUT: Use MODULE_CONTRACT, sharded docs, and execution context: Phase-50 adds hooks audit --json and RTK release-gate economics while preserving the existing clap facade.
      - PROCESSING: Implement only the declared scope while preserving dependencies: M-CLI-SETUP-COMMANDS, M-CLI-CODE-COMMANDS, M-CLI-CONFIG-COMMANDS, M-CLI-FILTER-COMMANDS, M-CLI-GRACE-COMMANDS, M-CLI-RUNTIME-COMMANDS, M-CLI-RTK-COMMANDS
      - OUTPUT: Updated code/artifacts that verify against MyGRACE gates
    Critical invariants:
      - Scope remains bounded to: SynCli, Command enum, command argument structs including explicit config subcommands, GRACE --profile flags, expanded RTK proxy shortcuts including container shortcuts, local RTK adapters, local RTK system adapters, discover/learn diagnostics, hooks audit --json, rtk-parity inventory gate, doctor --deps, gain --graph/--sessions/--adapters, proxy --route, filters verify --list, filters dry-run, stdio-only mcp command, and CI action enum
      - MODULE_CONTRACT, MODULE_MAP, CHANGE_SUMMARY, and semantic anchors stay synchronized
  IMPLEMENTATION_STRATEGY:
    Approach: Read sharded artifacts first, preserve public contracts, make scoped edits, then run verification gates
    Complexity_areas: artifact drift, backward compatibility, verification coverage
    Patterns_applied: explicit data flow, typed semantic links, bounded semantic blocks
  VERIFICATION_INTENT:
    I expect the following to be true after my code runs:
    - belief-state-exists reports no malformed BELIEF_STATE blocks
    - syn gh, syn aws, syn go, syn make, syn helm, and syn kubectl parse as first-class commands with --route support
    - syn pipe, syn log, and syn smart parse as local RTK system adapters
    - syn ruff, syn mypy, syn uv, syn tsc, syn vitest, and syn gradlew parse as language ecosystem shortcuts
    - syn docker and syn podman parse as RTK container shortcuts with --route support
    - syn discover and syn learn parse as bounded RTK diagnostics with JSON output support
    - syn hooks audit --json parses for RTK trust diagnostics
    - syn filters dry-run parses --filter, --input, --show-input, and --format json
    - verify_project, review_code, and relevant unit tests pass after implementation
  RISKS_ACKNOWLEDGED:
    - This scaffold is generated from artifacts and should be refined by the implementing agent before substantial code generation
    - Incomplete module artifacts can make the first belief state too generic
</BELIEF_STATE>
