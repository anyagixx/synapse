<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-CI" module="M-CI" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>python3 scripts/ci_runtime_guard.py --self-test</COMMAND>
    <COMMAND>python3 scripts/ci_runtime_guard.py</COMMAND>
    <COMMAND>bash scripts/release_install_smoke.sh</COMMAND>
    <COMMAND>bash scripts/ci.sh</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS>
    <MARKER>[CI][run_ci_gate][RUNTIME_GUARD]</MARKER>
    <MARKER>[CI][run_ci_gate][RELEASE_SMOKE]</MARKER>
    <MARKER>[CI][runtime_guard][SCAN]</MARKER>
  </REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>CI blocks production .unwrap(), .expect(), panic!, todo!, and unimplemented! markers outside test-only Rust blocks.</ASSERTION>
    <ASSERTION>CI validates the Linux/macOS release matrix tarball layout, checksum sidecars, and packaged binary execution before release.</ASSERTION>
    <ASSERTION>Hosted platform-test gate is limited to Linux/macOS for Phase-5 while Windows packaging remains deferred.</ASSERTION>
    <ASSERTION>CI and release workflows pin Node 24-ready action majors for checkout, artifact upload, and GitHub release publishing; release artifact download uses GitHub CLI to avoid Node runtime deprecation noise.</ASSERTION>
    <ASSERTION>CI and release workflows install Rust through rustup shell steps to avoid third-party Node runtime deprecation noise.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-6</PHASE_GATE>
</VERIFICATION>
