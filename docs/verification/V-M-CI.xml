<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-CI" module="M-CI" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>python3 scripts/ci_runtime_guard.py --self-test</COMMAND>
    <COMMAND>python3 scripts/ci_runtime_guard.py</COMMAND>
    <COMMAND>cargo audit --deny warnings --ignore RUSTSEC-2024-0436</COMMAND>
    <COMMAND>bash scripts/release_freshness_guard.sh</COMMAND>
    <COMMAND>SYN_RC_SKIP_SMOKE=1 SYN_RC_SKIP_FULL_RTK=1 SYN_RC_SKIP_FRESHNESS=1 bash scripts/release_candidate_dry_run.sh</COMMAND>
    <COMMAND>cargo test --test release_candidate</COMMAND>
    <COMMAND>bash scripts/token_economy_gate.sh</COMMAND>
    <COMMAND>cargo test --test e2e_mcp token_economy</COMMAND>
    <COMMAND>cargo bench --bench search</COMMAND>
    <COMMAND>cargo bench --bench graph</COMMAND>
    <COMMAND>cargo bench --bench cascade</COMMAND>
    <COMMAND>bash scripts/release_install_smoke.sh</COMMAND>
    <COMMAND>bash scripts/ci.sh</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS>
    <MARKER>[CI][run_ci_gate][RUNTIME_GUARD]</MARKER>
    <MARKER>[CI][run_ci_gate][RELEASE_VERSION]</MARKER>
    <MARKER>[CI][run_ci_gate][RELEASE_FRESHNESS]</MARKER>
    <MARKER>[CI][run_ci_gate][RELEASE_CANDIDATE]</MARKER>
    <MARKER>[CI][run_ci_gate][TOKEN_ECONOMY]</MARKER>
    <MARKER>[CI][token_economy][MCP_PROTOCOL]</MARKER>
    <MARKER>[CI][run_ci_gate][RELEASE_SMOKE]</MARKER>
    <MARKER>[CI][release_candidate][PASS]</MARKER>
    <MARKER>[CI][release_candidate][SUMMARY]</MARKER>
    <MARKER>[CI][release_freshness_guard][PASS]</MARKER>
    <MARKER>[CI][fresh_install_smoke][PASS]</MARKER>
    <MARKER>[CI][runtime_guard][SCAN]</MARKER>
  </REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>CI blocks production .unwrap(), .expect(), panic!, todo!, and unimplemented! markers outside test-only Rust blocks.</ASSERTION>
    <ASSERTION>CI isolates XDG_DATA_HOME under its temporary directory when no caller-provided value exists.</ASSERTION>
    <ASSERTION>CI validates the supported Linux/macOS x86_64/aarch64 release matrix tarball layout, checksum sidecars, and packaged binary execution before release.</ASSERTION>
    <ASSERTION>Push CI captures hosted Linux/macOS fresh-install evidence through the public raw installer URL pinned to the workflow commit, SYN_INSTALL_SOURCE_REF, and temporary SYN_INSTALL_DIR.</ASSERTION>
    <ASSERTION>Release freshness guard fails when Cargo.toml points at an already-published tag that does not match HEAD, and requires exact tag checkout in release tag context.</ASSERTION>
    <ASSERTION>Release workflow rejects tag/version mismatches and publishes generated release notes.</ASSERTION>
    <ASSERTION>Release-candidate workflow checks out the requested candidate ref, validates tag/version policy, generated release notes, checksum aggregation, installer dry-run mapping, fresh Linux/macOS installer smoke, and supported Linux/macOS x86_64/aarch64 release smoke without write permissions or release publishing actions.</ASSERTION>
    <ASSERTION>Release-candidate dry-run writes GitHub Step Summary evidence for tag, commit, Cargo version, checksum policy, installer matrix, smoke status, and non-publishing status.</ASSERTION>
    <ASSERTION>Hosted security CI runs cargo audit --deny warnings with a documented RUSTSEC-2024-0436 waiver for the transitive fastembed/tokenizers paste warning; notify 8.2.0 removes the previous transitive instant warning.</ASSERTION>
    <ASSERTION>Hosted platform-test gate is limited to Linux/macOS for Phase-5 while Windows packaging remains deferred.</ASSERTION>
    <ASSERTION>CI and release workflows pin Node 24-ready action majors for checkout, artifact upload, and GitHub release publishing; release artifact download uses GitHub CLI to avoid Node runtime deprecation noise.</ASSERTION>
    <ASSERTION>CI and release workflows install Rust through retrying rustup shell steps to avoid third-party Node runtime deprecation noise and absorb transient network lookup failures; hosted main CI run 26120689647 completed successfully without Node/deprecation log markers.</ASSERTION>
    <ASSERTION>UPGRADE_4 token economy CI gate covers tools/list profiles, terse schemas, response trimming, cache hints, tools/recommend, compact_evidence, check_budget, and context_pressure.</ASSERTION>
    <ASSERTION>Criterion benchmark smoke runs search, graph, and cascade groups in a non-release-blocking hosted job.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-12,Phase-88,Phase-89</PHASE_GATE>
</VERIFICATION>
