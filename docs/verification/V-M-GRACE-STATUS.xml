<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-GRACE-STATUS" module="M-GRACE-STATUS" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>cargo test grace::status::tests::test_active_phase_display_value_hides_completed_phase --lib</COMMAND>
    <COMMAND>cargo test grace::status::tests::test_rtk_adoption_gaps_are_advisory --lib</COMMAND>
    <COMMAND>cargo run --quiet -- status --json</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS></REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>Status output reports no active phase when plan-index ACTIVE_PHASE points at a completed phase shard.</ASSERTION>
    <ASSERTION>Status JSON and text output include belief state coverage and invalid belief state counts.</ASSERTION>
    <ASSERTION>Status JSON and text output include RequirementsAnalysis entity/use-case counts and validity.</ASSERTION>
    <ASSERTION>Status JSON and text output include Technology component/compatibility counts and validity.</ASSERTION>
    <ASSERTION>Status JSON and text output include DevelopmentPlan DataFlow, GenerationOrder, and completed module counts.</ASSERTION>
    <ASSERTION>Status JSON and text output include MentalTests total, passed, failed, and required target counts.</ASSERTION>
    <ASSERTION>Status JSON and text output include non-human pattern score, files scanned, error count, and warning count.</ASSERTION>
    <ASSERTION>Status JSON and text output include RTK adoption diagnostics as advisory metadata that does not weaken MyGRACE gates.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-49</PHASE_GATE>
</VERIFICATION>
