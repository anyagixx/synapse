<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-PROXY" module="M-PROXY" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>cargo test --test integration_test test_proxy_preserves_nonzero_exit_status</COMMAND>
    <COMMAND>cargo test --test integration_test test_proxy_unicode_passthrough_truncates_without_panic</COMMAND>
    <COMMAND>cargo test --test integration_test test_proxy_tracking_records_positive_savings_and_gain_graph</COMMAND>
    <COMMAND>cargo test --test integration_test test_tracking_is_scoped_by_canonical_project_path</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS>
    <MARKER>[Proxy][execute][TRACKING]</MARKER>
  </REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>Proxy command execution continues when tracking degrades, while emitting a structured warning.</ASSERTION>
    <ASSERTION>Proxy preserves the wrapped command exit status while still emitting filtered output.</ASSERTION>
    <ASSERTION>Proxy passthrough truncation is Unicode-safe and does not split UTF-8 characters.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-3</PHASE_GATE>
</VERIFICATION>
