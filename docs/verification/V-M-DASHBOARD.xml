<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-DASHBOARD" module="M-DASHBOARD" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>cargo test dashboard --all-targets</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS></REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>Dashboard exposes JSON payloads for belief states, MentalTests, traceability, run queue/blocked state, selected-run provenance, graph-aware search explanation, cascade impact, and cascade history.</ASSERTION>
    <ASSERTION>Dashboard renders pages for /belief-states, /belief-state/{module_id}, /mental-tests, /runs, /runs/queue, /search-explain, /traceability/{artifact_id}, /cascade/preview, and /cascade/history.</ASSERTION>
    <ASSERTION>Run cockpit payload classifies queued, blocked, and completed runs and renders blocked reasons for agent workflow triage.</ASSERTION>
    <ASSERTION>Cascade preview routes use M-GRACE-CASCADE as the data source and preserve JSON validity.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-18</PHASE_GATE>
</VERIFICATION>
