<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-RTK-FULL-PARITY" module="M-RTK-FULL-PARITY" priority="critical" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test cli::rtk_full_parity::tests --lib</COMMAND>
    <COMMAND>cargo run --quiet -- rtk-parity --source ../GRACEme/rtk-develop --full</COMMAND>
    <COMMAND>cargo run --quiet -- rtk-parity --source ../GRACEme/rtk-develop --full --json</COMMAND>
    <COMMAND>cargo run --quiet -- rtk-parity --source ../GRACEme/rtk-develop --ci</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS></REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>Full parity mode must be source-derived from rtk-develop instead of a manually maintained RTK command list.</ASSERTION>
    <ASSERTION>Full parity mode must distinguish implemented, equivalent, superseded, and missing RTK coverage.</ASSERTION>
    <ASSERTION>Integration parity --ci remains backwards-compatible and does not fail because future full-RTK work is still planned.</ASSERTION>
    <ASSERTION>Full parity --ci fails while any RTK command, hook processor, or command-module surface remains missing.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-51</PHASE_GATE>
</VERIFICATION>
