<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-MCP" module="M-MCP" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>cargo test mcp::lsp_manager::tests --lib</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS></REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>MCP module exports the persistent LSP manager beside the LSP client and server facade.</ASSERTION>
    <ASSERTION>MCP module declares the bounded run tools helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the predictive tool recommendation helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the budget tools helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the context pressure helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the telemetry helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the pressure-aware tool schema helper module beside existing server helpers.</ASSERTION>
    <ASSERTION>MCP module declares the user-defined tool helper module beside existing server helpers.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-59,Phase-73,Phase-86,Phase-87,Phase-88,Phase-92,Phase-93</PHASE_GATE>
</VERIFICATION>
