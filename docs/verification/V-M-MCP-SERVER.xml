<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-MCP-SERVER" module="M-MCP-SERVER" priority="normal" status="active">
  <UNIT_TESTS>
    <COMMAND>cargo test --all-targets</COMMAND>
    <COMMAND>cargo test --test mcp_protocol</COMMAND>
    <COMMAND>cargo test mcp::server_code_tools::tests::test_handle_graphrag_reports_poisoned_lock --lib</COMMAND>
  </UNIT_TESTS>
  <REQUIRED_LOG_MARKERS>
    <MARKER>[SynapseHandler][new][INDEX]</MARKER>
    <MARKER>[SynapseHandler][new][GRAPHRAG]</MARKER>
  </REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>MCP runtime preload converts lock and GraphRAG build failures into warnings instead of panics.</ASSERTION>
    <ASSERTION>syn mcp stdout contains only JSON-RPC responses while tracing logs are emitted on stderr.</ASSERTION>
    <ASSERTION>syn mcp public help remains stdio-only and does not expose HTTP or LSP flags before those transports are implemented.</ASSERTION>
    <ASSERTION>JSON-RPC notifications do not produce response objects.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes analyze_logs to the GRACE structured LOG analyzer.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes extract_belief_state to the GRACE belief-state artifact writer.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes generate_requirements to the GRACE RequirementsAnalysis writer.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes generate_technology to the GRACE Technology writer.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes generate_development_plan to the GRACE DevelopmentPlan writer.</ASSERTION>
    <ASSERTION>JSON-RPC tool dispatch routes mental_test_run to the GRACE MentalTest runner.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-12</PHASE_GATE>
</VERIFICATION>
