<?xml version="1.0" encoding="UTF-8"?>
<VERIFICATION id="V-M-CLI-TEST-COMMANDS" module="M-CLI-TEST-COMMANDS" priority="critical" status="active">
  <UNIT_TESTS>
    <TEST>CLI parses syn test e2e, mcp, snapshot, coverage, perf, contract, and resilience actions.</TEST>
    <TEST>Legacy syn test -- &lt;cmd&gt; continues to route to the RTK compact test adapter.</TEST>
    <TEST>CLI preserves e2e --scenario paths and snapshot --update/--filter options.</TEST>
  </UNIT_TESTS>
  <INTEGRATION_TESTS>
    <TEST>cargo test --all-targets covers command parsing and dispatch behavior.</TEST>
    <TEST>syn test coverage --json emits machine-readable matrix output.</TEST>
    <TEST>syn test snapshot and syn test snapshot --json emit bounded snapshot reports.</TEST>
    <TEST>syn test e2e without --scenario keeps planned output while --scenario runs the E2E runner.</TEST>
    <TEST>syn test mcp invokes cargo test --test e2e_mcp and emits bounded text or JSON reports.</TEST>
    <TEST>syn test coverage --json builds the real coverage matrix.</TEST>
    <TEST>syn test contract &lt;spec&gt; runs real contract differential specs.</TEST>
  </INTEGRATION_TESTS>
  <REQUIRED_LOG_MARKERS>
    <LOG>test_command_dispatch</LOG>
    <LOG>legacy_rtk_test_fallback</LOG>
  </REQUIRED_LOG_MARKERS>
  <TRACE_ASSERTIONS>
    <ASSERTION>CLI schema links to M-TEST-HARNESS and all UPGRADE_3 test modules.</ASSERTION>
  </TRACE_ASSERTIONS>
  <PHASE_GATE>Phase-76,Phase-77,Phase-78,Phase-79</PHASE_GATE>
</VERIFICATION>
