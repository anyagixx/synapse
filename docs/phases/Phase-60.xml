<?xml version="1.0" encoding="UTF-8"?>
<PHASE id="Phase-60" status="done">
  <NAME>Filter Engine Semantics And Shared ANSI</NAME>
  <GOAL>Resolve FIX/C3 and FIX/M3 by centralizing ANSI stripping and making command match semantics explicit.</GOAL>
  <SOURCE_PLAN>/home/truffle/Загрузки/synapse-main/FIX/CRITICAL/C3-duplicate-ansi.md,/home/truffle/Загрузки/synapse-main/FIX/MEDIUM/M3-match-cmd-ambiguity.md</SOURCE_PLAN>
  <MODULE_REFS>
    <MODULE_REF id="M-PROXY-FILTER" />
    <MODULE_REF id="M-UTILS" />
  </MODULE_REFS>
  <DATA_FLOWS>
    <FLOW id="DF-60-1">FilterEngine delegates ANSI stripping to M-UTILS so proxy output and shared utility behavior cannot diverge.</FLOW>
    <FLOW id="DF-60-2">Filter definitions route literal command prefixes through match_command and regex command matching through match_command_regex.</FLOW>
  </DATA_FLOWS>
  <STEPS>
    <step-1 module="M-UTILS" status="done">Added ANSI stripping regression coverage for common SGR sequences.</step-1>
    <step-2 module="M-PROXY-FILTER" status="done">Removed duplicate ANSI regex state and delegated filter application to crate::utils::strip_ansi.</step-2>
    <step-3 module="M-PROXY-FILTER" status="done">Added match_command_regex, validation, migration warnings, and migrated RTK built-ins to explicit regex matching.</step-3>
  </STEPS>
  <VERIFICATION>
    <CHECK status="passed">rg -n "ANSI_RE|ANSI_PATTERN|fn remove_ansi" src — ANSI regex state is present only in src/utils/mod.rs; no proxy remove_ansi duplicate remains.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo test utils::tests --lib — 2 passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo test proxy::toml_filter::tests --lib — 18 passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo run --quiet -- filters verify — all inline filter tests passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo test --all-targets -- --test-threads=8 — 307 passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo clippy --all-targets --all-features -- -D warnings — no issues.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo run --quiet -- verify --ci — passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo run --quiet -- review --mode full --ci — passed.</CHECK>
    <CHECK status="passed">XDG_DATA_HOME=/tmp/synapse-phase60-data cargo run --quiet -- refresh — no drift.</CHECK>
  </VERIFICATION>
  <RISKS>
    <RISK id="R-60-1">Existing user filters may rely on regex-like match_command behavior; preserve compatibility with warnings before making any breaking change.</RISK>
  </RISKS>
</PHASE>
